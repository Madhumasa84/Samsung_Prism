# Pinned core runtime dependencies for Python 3.11. The optional dense extra
# and its transitive dependencies are fully locked in uv.lock and can be
# installed with `uv sync --locked --extra dense`.
annotated-types==0.8.0
pydantic==2.13.5
pydantic-core==2.46.5
typing-extensions==4.16.0
typing-inspection==0.4.4
